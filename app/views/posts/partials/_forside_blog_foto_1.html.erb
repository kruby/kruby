<% for post in @posts %>



<div class="forside_blog_wrapper">
		
<div class="blogpost">
	
	<div class="topline">
	<span class="title">
	<%#= post.id %><%= post.title %><%#= truncate(post.title, :length => 35) %></span>
	</div> <!-- topline -->
	<div class="date_time"><%= post.created_at.strftime("%d.%m.%y - %H:%M") %> - <span class="author"><%= User.find(post.user_id).blogname rescue 'Kruby' %></span></div>
	
	
	
	
		<!-- <div class="body">
			<td class='tekst_foto_1'><%#= textilize(post.body) %></td>
			<td class='foto_1'> </td>
		</div> -->
		
		<div class="body">
			<% @length = 350 %>
			<td class='tekst_foto_1'><%= truncate(textilize(post.body), :length => @length, :separator => ' ' ).html_safe %></td>
			<% if @length <= post.body.length %><br>
			<%= link_to '(læs mere)', post_path(post.id), :class => 'links' %>
			<% end %>
			<td class='foto_1'> </td>
		</div>
		
	<div class="bottomline"></div>
	
	</div> <!-- blogpost -->
	
</div> <!-- wrapper -->


<% end %>
