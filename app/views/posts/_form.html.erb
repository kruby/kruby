<%= form_for(@post) do |f| %>

<div class="button"><%= f.submit "[Opret]", :class => 'button '%></div>

  <% if @post.errors.any? %>
    <div id="error_explanation">
	<h2><%= @post.errors.count %>Følgende fejl skal rettes</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field(:user_id, :value => current_user.id) %>

  <div class="label"><%= f.label :Titel %></div>
  <div class="input"><%= f.text_field :title %></div>

  <div class="label"><%= f.label :Indhold %></div>
  <div class="input"><%= f.text_area :body, :size => '60x18' %></div>
	

<% end %>